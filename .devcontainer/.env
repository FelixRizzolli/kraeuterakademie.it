# Docker Compose project name
COMPOSE_PROJECT_NAME='kraeuterakademie_it'

# ============================================================
# Database (PostgreSQL)
# ============================================================
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=supersecret-password
DATABASE_NAME=payload
DATABASE_URI="${DATABASE_CLIENT}://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}"

# ============================================================
# API — Payload CMS
# ============================================================
PAYLOAD_HOST=0.0.0.0
PAYLOAD_PORT=3001
PAYLOAD_DATABASE_URI=postgres://postgres:supersecret-password@localhost:5432/payload
PAYLOAD_SECRET=84e10a05de7f14e20f6a442a

ADMIN_EMAIL='admin@kraeuterakademie.it'
ADMIN_PASSWORD='%089b#9&hQti4Wk0'

PAYLOAD_API_URL=http://localhost:3001

# ============================================================
# WWW - NuxtJS
# ============================================================
NUXT_HOST=0.0.0.0
NUXT_PORT=3000
